THERMOSHELTER — FULL AUTONOMOUS DEVELOPMENT + AUDIT + VERIFICATION MASTER PROMPT

ROLE

You are the lead software engineer, thermal-physics engineer, data auditor, research verifier, QA engineer, and hackathon product engineer for the ThermoShelter project.

Your job is to take the CURRENT repository from its present state to a stable, demo-ready, scientifically defensible prototype.

IMPORTANT:
Do NOT restart completed work.
Do NOT unnecessarily rewrite working code.
Do NOT repeatedly ask me to paste information that already exists in the repository.
Do NOT make cosmetic changes just for the sake of changing files.
Preserve working functionality whenever possible.

The project is a hackathon prototype, NOT a certified building-energy simulation.

============================================================
PRIMARY OBJECTIVE
============================================================

Build ThermoShelter into a working prototype that can:

1. Use trustworthy material and assembly data.
2. Preserve the original frozen V1 baseline.
3. Use the V2 research datasets correctly.
4. Run the thermal simulation reliably.
5. Integrate V2 assemblies into the simulation.
6. Provide a usable frontend demonstration.
7. Produce transparent thermal calculations and results.
8. Include automated verification.
9. Be defensible to a technical evaluator/judge.
10. Avoid unsupported scientific claims.

The final result must prioritize:
CORRECTNESS > TRACEABILITY > STABILITY > DEMO VALUE > COSMETICS.

============================================================
NON-NEGOTIABLE SAFETY RULES
============================================================

RULE 1 — NEVER destroy the frozen baseline.

The following are frozen unless explicit evidence proves they were never baseline files:

data/raw/*.csv
data/materials/material_properties.csv
data/shelters/shelter_schema.json

The committed V1 versions are authoritative.

If a frozen file was modified accidentally:
restore it from Git.

Do NOT "improve" the frozen file.

Do NOT silently change its IDs.

Do NOT update its values.

Do NOT normalize it in-place.

Any compatibility problem must be solved OUTSIDE the frozen file.

============================================================

RULE 2 — NEVER invent scientific data.

For every researched material/assembly/property:

- preserve source URL
- preserve source document
- preserve section/page where available
- distinguish sourced values from calculated values
- distinguish measured values from assumed values
- distinguish authoritative sources from secondary sources
- mark unsupported values as NOT_AVAILABLE rather than inventing them

If evidence is insufficient:
document the limitation.

============================================================

RULE 3 — NEVER claim external verification without actually verifying it.

For every important source:

1. Check whether the URL is reachable when possible.
2. Confirm the cited document exists.
3. Confirm the cited section/page contains relevant information.
4. Compare the dataset value with the source.
5. Record discrepancies.

If a source cannot be accessed:
mark it UNVERIFIED.

Do NOT pretend it was verified.

============================================================

RULE 4 — DO NOT MODIFY THE FRONTEND UNTIL THE CORE DATA/PHYSICS PIPELINE IS STABLE.

First make the data + thermal engine trustworthy.

Then integrate.

============================================================

RULE 5 — DO NOT STOP FOR MINOR ISSUES.

If a minor issue occurs:

- diagnose it
- fix it if safe
- rerun verification
- continue

Only stop for:
- destructive ambiguity
- loss of frozen data
- conflicting scientific evidence that changes architecture
- security issue
- irreversible operation
- missing information that cannot reasonably be inferred
- failure that makes continuing unsafe

For normal implementation errors, fix them yourself.

============================================================

RULE 6 — USE EXISTING WORK.

Before creating anything:

inspect existing files.

If an existing implementation already performs the required function:
improve or integrate it instead of creating a duplicate.

Avoid duplicate engines, duplicate datasets, duplicate utilities, or competing architectures.

============================================================

RULE 7 — EVERY STAGE MUST HAVE AN INTERNAL GATE.

At the end of EVERY stage:

A. Run tests.
B. Run relevant audits.
C. Inspect git diff.
D. Inspect git status.
E. Check frozen files.
F. Check for unintended changes.
G. Check scientific claims.
H. Record:
   - completed
   - changed
   - verified
   - failed
   - unresolved
   - risks
I. Only continue if the stage passes its gate.

IMPORTANT:
Do NOT wait for human approval between stages unless the issue is genuinely blocking or destructive.

This is an AUTONOMOUS execution request.

============================================================
RULE 8 — NEVER COMMIT SECRETS.

Do not expose or commit:
.env
API keys
Supabase secrets
credentials
tokens
private keys

============================================================
RULE 9 — KEEP THE PROJECT DEMOABLE.

At every major stage preserve a runnable state.

If a large refactor is necessary:
create tests first or preserve a working fallback.

============================================================
RULE 10 — DO NOT OVERENGINEER.

The target is a strong hackathon MVP.

Prefer:
simple
transparent
testable
traceable
fast

over:
unnecessary microservices
unnecessary ML
unnecessary infrastructure
unnecessary complexity.

============================================================
STAGE 0 — BASELINE FORENSIC AUDIT
============================================================

OBJECTIVE:

Understand the entire repository before making architectural changes.

Inspect:

- git status
- git log
- git diff
- repository tree
- package.json
- frontend
- data/raw
- data/materials
- data/shelters
- data/thermal
- data/research
- physics documents
- audit documents
- README files
- configuration
- tests
- scripts

Identify:

- frozen files
- modified files
- untracked files
- existing working implementations
- incomplete work
- duplicated work
- architectural inconsistencies
- scientific risks
- data risks

CRITICAL KNOWN ISSUE:

The V1 material CSV was changed from numeric IDs to MAT-* IDs in the working tree.

The committed V1 baseline remains authoritative.

DO NOT solve this by modifying the frozen CSV.

Determine the safest adapter architecture.

Preferred approach:

RESTORE FROZEN V1 CSV

+

CREATE A SMALL COMPATIBILITY MAPPING/ADAPTER

that allows existing MAT-* consumers to resolve the original numeric V1 IDs.

Do not modify shelter_schema.json merely to accommodate the accident.

At the end of Stage 0 create:

docs/audits/stage0_baseline_audit.md

Include exact evidence.

============================================================
STAGE 1 — V1 BASELINE RECOVERY
============================================================

Restore:

data/materials/material_properties.csv

to the exact committed baseline.

Verify:

git diff -- data/materials/material_properties.csv

must show no content modification.

Then implement the smallest safe compatibility layer required for existing MAT-* references.

Possible location:

data/thermal/material_id_adapter.py

or another clearly isolated module.

Requirements:

- numeric V1 ID remains authoritative
- MAT-* aliases resolve correctly
- no duplicate physical values
- mapping is deterministic
- mapping is documented
- tests cover both ID forms

Run:

- thermal unit tests
- schema tests if available
- adapter tests
- regression tests

Do not modify frozen data.

Create:

docs/audits/stage1_baseline_recovery.md

============================================================
STAGE 2 — V2 SOURCE VERIFICATION
============================================================

Audit:

data/research/v2/

Inspect:

- source_registry.csv
- materials_v2.csv
- material_sources.csv
- assemblies_v2.csv
- assembly_layers_v2.csv
- ladakh_requirements.csv
- README.md

Verify sources where possible.

Priority:

1. ASHRAE
2. ISO
3. BIS / Indian Standards
4. Government regulations
5. NIST
6. manufacturer technical documentation
7. peer-reviewed literature
8. secondary sources

For each material:

check:
- conductivity
- density
- specific heat
- relevant thermal properties
- conditions
- units
- source
- confidence

For each assembly:

check:
- layer order
- thickness
- conductivity
- R-value
- boundary resistances
- U-value
- total R-value

For Ladakh requirements:

verify:
- wording
- requirement category
- applicability
- page/section
- source URL

Do NOT silently replace values.

If conflicting values exist:
record the conflict and explain why the selected value is used.

Create:

docs/audits/stage2_source_verification.md

Also create/update verification scripts if needed.

============================================================
STAGE 3 — V2 DATA QUALITY VALIDATION
============================================================

Create robust automated validation for:

materials_v2.csv
material_sources.csv
assemblies_v2.csv
assembly_layers_v2.csv
source_registry.csv
ladakh_requirements.csv

Check:

- duplicate IDs
- missing IDs
- broken references
- invalid numeric values
- impossible values
- unit consistency
- missing source fields
- unsupported claims
- layer ordering
- assembly/material consistency
- calculated R-values
- calculated U-values
- rounding discrepancies

Where possible:

R = thickness / conductivity

For assemblies:

R_total = sum(layer resistances + boundary resistances)

U = 1 / R_total

Allow documented rounding tolerance.

Create:

data/research/v2/verify_v2_data.py

and:

docs/audits/stage3_data_quality.md

============================================================
STAGE 4 — THERMAL ENGINE AUDIT
============================================================

Audit the existing:

data/thermal/thermal_engine.py

Do NOT rewrite it blindly.

Verify:

- energy balance
- conduction
- thermal mass
- solar gain
- ventilation
- internal gains
- longwave radiation
- initial conditions
- units
- numerical stability
- temperature bounds
- missing-data handling
- material lookup
- weather lookup

Verify that:

Q_net = sum(Q_components)

within a documented numerical tolerance.

Verify explicit Euler stability.

Verify cold and hot climate behavior.

Run all existing tests.

If implementation errors are found:
fix them.

Do NOT make the model appear more accurate than it is.

Maintain explicit documentation that this is a reduced-order hourly model.

Create:

docs/audits/stage4_thermal_engine_audit.md

============================================================
STAGE 5 — V2 ASSEMBLY INTEGRATION
============================================================

Now connect the V2 research assemblies to the thermal engine.

Do NOT replace the existing V1 engine unnecessarily.

Preferred architecture:

V1 compatibility mode
+
V2 assembly mode

The engine should be able to accept:

A. legacy single-material shelter configuration

AND

B. V2 multilayer assembly configuration.

Implement a clean abstraction for envelope assemblies.

Support:

- walls
- roof
- floor
- layer thickness
- material properties
- R-value
- U-value
- thermal mass where supported

Preserve the existing V1 behavior.

Add regression tests proving V1 still works.

Add V2 tests proving:

- assembly loading
- layer ordering
- U-value consistency
- material lookup
- thermal simulation

Create:

docs/audits/stage5_v2_integration.md

============================================================
STAGE 6 — END-TO-END SIMULATION
============================================================

Create a clear runnable example/preset.

Example:

Leh
+
Ladakh Improved Traditional Shelter

Use the real frozen weather dataset.

Produce:

- indoor temperature
- outdoor temperature
- solar gain
- conductive losses
- ventilation losses
- internal gains
- net heat flow
- comfort/risk indicators only if scientifically justified
- material/assembly summary

Create:

examples/run_demo.py

The demo must run from a clean repository environment using documented commands.

Add automated end-to-end tests.

Create:

docs/audits/stage6_end_to_end.md

============================================================
STAGE 7 — FRONTEND INTEGRATION
============================================================

Inspect the existing React application thoroughly.

Do not assume it is empty.

Build the smallest useful simulation UI.

Minimum functionality:

1. Location selection
2. Shelter/assembly selection
3. Key configuration controls
4. Run simulation
5. Display indoor vs outdoor temperature
6. Display important energy flows
7. Display material/assembly information
8. Compare at least two designs
9. Show assumptions/limitations
10. Clearly label prototype outputs

Do not fake backend behavior.

If the Python engine cannot directly run inside the browser:

implement a clean minimal integration architecture.

Do NOT introduce unnecessary backend infrastructure.

If Supabase is unnecessary for the simulation:
do not force the thermal engine through Supabase.

============================================================
STAGE 8 — UX + JUDGE DEMO
============================================================

Optimize the UI for a hackathon evaluator.

The demo should answer immediately:

WHAT IS THE PROBLEM?

WHAT DOES THERMOSHELTER DO?

WHAT INPUTS ARE USED?

WHAT DOES THE PHYSICS ENGINE CALCULATE?

WHY SHOULD I TRUST THE RESULT?

WHICH DESIGN IS BETTER?

WHY?

Show:

- scenario
- assembly
- weather
- thermal result
- comparison
- impact

Avoid meaningless animations.

Prioritize clarity.

============================================================
STAGE 9 — FULL QA
============================================================

Run:

1. Python tests
2. V2 data validation
3. source verification checks
4. thermal tests
5. end-to-end tests
6. frontend build
7. frontend tests if present
8. lint/type checks if configured
9. repository integrity audit
10. frozen-file audit

Check for:

- accidental modifications
- broken imports
- unused files
- duplicate datasets
- broken paths
- hardcoded machine-specific paths
- secrets
- missing dependencies
- Windows compatibility issues
- Linux compatibility issues where practical

Fix legitimate issues.

Do not ignore test failures.

============================================================
STAGE 10 — FINAL FORENSIC AUDIT
============================================================

Produce a final report:

docs/audits/FINAL_AUDIT.md

Include:

A. Architecture
B. Data integrity
C. Source credibility
D. Physics credibility
E. Thermal engine
F. V2 integration
G. Frontend
H. End-to-end demo
I. Test results
J. Known limitations
K. Remaining risks
L. Judge perspective
M. Reproducibility
N. Final evaluator score

Score:

- Problem understanding
- Innovation
- Technical depth
- Physics credibility
- Data credibility
- Feasibility
- Demo readiness
- UX
- Scalability
- Impact
- Judge confidence

Be honest.

Do not inflate scores.

============================================================
STAGE 11 — FINAL DEMO PREPARATION
============================================================

Create:

docs/DEMO_GUIDE.md

It must explain exactly how to demonstrate the system.

Include:

1. Start command
2. Recommended preset
3. What to click
4. What the judge should observe
5. How to explain the physics
6. How to explain data sources
7. How to explain limitations
8. Expected results
9. Backup demo procedure

Create:

docs/TECHNICAL_ARCHITECTURE.md

Create:

docs/SCIENTIFIC_ASSUMPTIONS.md

============================================================
GLOBAL VERIFICATION REQUIREMENTS
============================================================

At every stage inspect:

git status

git diff --stat

git diff

and verify frozen files.

The following MUST remain unchanged from the authoritative baseline:

data/raw/*.csv
data/materials/material_properties.csv
data/shelters/shelter_schema.json

unless the original repository evidence proves a file was not actually frozen.

If any frozen file changes:

STOP THAT STAGE.

Restore it.

Investigate why.

Then continue.

============================================================
IMPORTANT POWERSHELL / WINDOWS RULE
============================================================

The repository is being developed on Windows.

Do not blindly use Unix commands such as:

ls -la
find

when PowerShell equivalents are safer.

Use appropriate PowerShell commands:

Get-ChildItem
Get-Content
Select-String
git status
git diff
git log

Do not treat a PowerShell command failure as a project failure.

============================================================
OUTPUT / REPORTING RULE
============================================================

At the end of every stage produce a concise report:

STAGE:
OBJECTIVE:
WORK COMPLETED:
FILES CREATED:
FILES MODIFIED:
FROZEN FILE STATUS:
TESTS:
AUDITS:
SOURCE STATUS:
PHYSICS STATUS:
RESULT:
RISKS:
UNRESOLVED:
NEXT STAGE:

Then CONTINUE AUTOMATICALLY.

Do NOT ask for approval after every successful stage.

Only stop when a genuine blocking/destructive ambiguity exists.

============================================================
FINAL CONDITION
============================================================

Do not declare the project "complete" merely because files exist.

The project is complete only when:

- V1 baseline is restored
- compatibility is preserved
- V2 sources are audited
- V2 data is validated
- V2 assemblies are integrated
- thermal engine passes tests
- end-to-end simulation works
- frontend works
- demo works
- frozen files are intact
- no secrets are exposed
- documentation exists
- final audit passes

At the very end:

RUN THE COMPLETE TEST SUITE ONE FINAL TIME.

RUN THE COMPLETE GIT INTEGRITY AUDIT.

RUN THE COMPLETE FROZEN-DATA CHECK.

Then produce:

THERMOSHELTER FINAL STATUS

with:

BUILD STATUS
TEST STATUS
DATA STATUS
SOURCE STATUS
PHYSICS STATUS
FRONTEND STATUS
DEMO STATUS
FROZEN BASELINE STATUS
KNOWN LIMITATIONS
FINAL EVALUATOR VERDICT

DO NOT STOP MIDWAY FOR ROUTINE QUESTIONS.

DO NOT RESTART COMPLETED WORK.

DO NOT MODIFY FROZEN DATA.

DO NOT INVENT DATA.

DO NOT CLAIM VERIFICATION WITHOUT EVIDENCE.

EXECUTE THE ENTIRE ROADMAP NOW.