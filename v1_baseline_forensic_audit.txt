THERMOSHELTER — V1 BASELINE FORENSIC AUDIT + SAFE RECOVERY PLAN
================================================================

ROLE
----
You are acting as a senior software architect, Git forensic auditor,
building-physics data engineer, and hackathon technical reviewer.

You are working inside the existing ThermoShelter repository.

Your job is to investigate the V1 baseline integrity problem and determine
the safest architecture for continuing V2 development.

THIS IS A FORENSIC / AUDIT TASK FIRST.

DO NOT MODIFY PROJECT FILES UNTIL EXPLICITLY AUTHORIZED.

DO NOT "FIX" ANYTHING AUTOMATICALLY.

DO NOT COMMIT.

DO NOT PUSH.

DO NOT RESET THE REPOSITORY.

DO NOT DELETE FILES.

DO NOT REWRITE DATA.

============================================================
1. PROJECT CONTEXT
============================================================

ThermoShelter is a shelter-design and thermal-analysis prototype focused
primarily on high-altitude cold regions such as Ladakh / Leh.

The repository contains:

- frozen V1 weather datasets
- frozen V1 material dataset
- V1 shelter configuration schema
- V1.1 thermal physics engine
- V2 research datasets
- React frontend
- GitHub remote

The project has evolved from a simple V1 prototype toward a more
research-backed V2 architecture.

A critical baseline-integrity issue has now been discovered.

============================================================
2. CRITICAL ISSUE DISCOVERED
============================================================

The file:

data/materials/material_properties.csv

was originally committed with numeric material IDs:

1
2
3
...
12

The current working version instead contains identifiers such as:

MAT-ADOBE
MAT-RAMMED
MAT-STONE
MAT-BRICK
MAT-CONCRETE
MAT-TIMBER
MAT-BAMBOO
MAT-THATCH
MAT-METAL
MAT-ROCKWOOL
MAT-EPS
MAT-XPS

Example original:

1,Adobe/Mud brick,...

Example current:

MAT-ADOBE,Adobe/Mud brick,...

This is potentially a violation of the project's V1 frozen-data rule.

However, the MAT-* identifiers may have been introduced because the newer
shelter schema and thermal engine expect material references in this format.

Therefore DO NOT simply revert the file without first understanding
dependency relationships.

============================================================
3. PRIMARY OBJECTIVE
============================================================

Determine:

A. What exactly the original frozen V1 dataset was.

B. When and why it changed.

C. Which files depend on numeric IDs.

D. Which files depend on MAT-* IDs.

E. Whether the current thermal engine would break if the V1 file is restored.

F. Whether the shelter schema would break if the V1 file is restored.

G. Whether a mapping/adaptor layer is the correct architecture.

H. What should be done WITHOUT compromising the historical V1 baseline.

============================================================
4. ABSOLUTE SAFETY RULES
============================================================

During this audit:

DO NOT MODIFY:

- data/materials/material_properties.csv
- data/shelters/shelter_schema.json
- data/thermal/thermal_engine.py
- data/thermal/test_thermal_engine.py
- data/thermal/run_tests_ascii.py
- data/research/v2/*
- data/raw/*
- src/*
- package.json
- configuration files

Do not create replacement versions.

Do not run commands that alter Git history.

Do not use:

git reset --hard
git clean -fd
git checkout -- <file>
git restore <file>

Do not commit.

Do not push.

Read-only investigation only.

If a command could modify anything, DO NOT run it.

============================================================
5. STEP ONE — INSPECT CURRENT GIT STATE
============================================================

Run read-only Git commands.

Determine:

- current branch
- current commit
- remote
- working-tree status
- modified files
- untracked files
- recent commits

Use commands appropriate for PowerShell/Windows.

Do not assume Linux commands such as `ls -la` or `find` will work.

Prefer PowerShell-compatible commands such as:

Get-ChildItem
Get-Content
git status
git log
git diff
git show

Report the results.

============================================================
6. STEP TWO — INVESTIGATE MATERIAL FILE HISTORY
============================================================

Investigate the complete Git history of:

data/materials/material_properties.csv

Determine:

1. First commit containing the file.
2. Commit where the original V1 baseline was established.
3. Every later commit that modified the file.
4. The first commit that introduced MAT-* identifiers.
5. Commit message associated with that change.
6. Author/date if available.
7. Whether the thermal property values changed.
8. Whether only IDs changed.
9. Whether column names changed.
10. Whether source URLs changed.
11. Whether notes changed.
12. Whether newline/line-ending changes occurred.

Use Git history and diffs.

Useful read-only commands may include:

git log --follow -- data/materials/material_properties.csv

git log -p --follow -- data/materials/material_properties.csv

git show <commit>:data/materials/material_properties.csv

Do not modify the file.

============================================================
7. STEP THREE — RECONSTRUCT ORIGINAL V1
============================================================

Identify the exact original V1 version.

Extract the original file from Git INTO MEMORY / OUTPUT ONLY.

Do NOT write it over the working file.

Determine:

- number of rows
- number of columns
- column names
- material ID format
- material names
- thermal conductivity
- density
- specific heat
- emissivity
- solar absorptivity
- solar reflectivity
- thickness
- thermal resistance
- source
- source URL
- source condition
- confidence
- notes

Compare the original version against the current working tree.

Produce a field-by-field summary.

============================================================
8. STEP FOUR — DETERMINE EXACT DIFFERENCE
============================================================

Create a forensic comparison.

Answer:

Is the modification ONLY:

numeric ID → MAT-* ID

or were any physical properties also changed?

Check every row and every column.

Do NOT assume.

Report:

- ID changes
- value changes
- source changes
- metadata changes
- formatting-only changes

If physical values are identical, explicitly state that.

If anything else changed, list it exactly.

============================================================
9. STEP FIVE — TRACE MATERIAL ID DEPENDENCIES
============================================================

Search the ENTIRE repository for:

MAT-ADOBE
MAT-RAMMED
MAT-STONE
MAT-BRICK
MAT-CONCRETE
MAT-TIMBER
MAT-BAMBOO
MAT-THATCH
MAT-METAL
MAT-ROCKWOOL
MAT-EPS
MAT-XPS

Also search for:

material_id
wall_material_id
roof_material_id
floor_material_id

Determine every file that expects MAT-* identifiers.

Especially inspect:

data/shelters/shelter_schema.json

data/thermal/thermal_engine.py

data/thermal/test_thermal_engine.py

data/thermal/run_tests_ascii.py

data/research/v2/*

Do not modify anything.

============================================================
10. STEP SIX — TRACE NUMERIC ID DEPENDENCIES
============================================================

Search for references to:

1
2
3
...
12

BUT DO NOT falsely classify arbitrary numbers as material IDs.

Look specifically for:

- material lookup logic
- dictionaries
- CSV lookup code
- material references
- shelter configuration examples
- tests
- fixtures
- documentation

Determine whether any existing code explicitly expects numeric IDs.

============================================================
11. STEP SEVEN — INSPECT SHELTER SCHEMA
============================================================

Read:

data/shelters/shelter_schema.json

Determine:

- material ID pattern
- examples
- validation rules
- required fields
- wall material reference
- roof material reference
- floor material reference

Determine whether it explicitly requires:

^MAT-[A-Z0-9_-]+$

If yes, report that.

Then determine whether this creates a compatibility conflict with
the original V1 dataset.

Do not change the schema.

============================================================
12. STEP EIGHT — INSPECT THERMAL ENGINE
============================================================

Read:

data/thermal/thermal_engine.py

Determine exactly how material properties are loaded.

Answer:

- What file is loaded?
- How is material_id matched?
- Does it expect numeric IDs?
- Does it expect MAT-* IDs?
- Is the material lookup strict?
- What happens when a material is missing?
- Does the engine validate the shelter schema?
- Does it rely on the current modified V1 CSV?

Trace the actual execution path.

Do not run destructive commands.

If safe, run read-only tests or imports.

Do not modify tests.

============================================================
13. STEP NINE — TEST COMPATIBILITY IN MEMORY
============================================================

DO NOT edit project files.

Instead, reason from the code or create temporary scratch scripts OUTSIDE
the repository's tracked project files if absolutely necessary.

The goal is to determine:

Scenario A:
Original numeric-ID V1 CSV
+
Current shelter schema
+
Current thermal engine

Does it work?

Scenario B:
Current MAT-* CSV
+
Current shelter schema
+
Current thermal engine

Does it work?

Scenario C:
Original V1 CSV
+
a hypothetical mapping layer
+
Current shelter schema
+
Current thermal engine

Would it work?

Do not implement the mapping yet.

Only determine feasibility.

============================================================
14. STEP TEN — INVESTIGATE V2 ARCHITECTURE
============================================================

Inspect:

data/research/v2/

Especially:

materials_v2.csv
material_sources.csv
assemblies_v2.csv
assembly_layers_v2.csv
source_registry.csv
ladakh_requirements.csv
README.md

Determine:

- whether V2 uses MAT-* IDs
- whether V2 materials duplicate V1 materials
- whether V2 introduces different IDs
- whether V2 assemblies reference materials
- whether V2 is currently connected to the thermal engine
- whether V2 can coexist cleanly with frozen V1 data

Do not modify anything.

============================================================
15. STEP ELEVEN — GIT BASELINE INTEGRITY
============================================================

Determine what the repository considers the V1 checkpoint.

Earlier repository information indicates a commit similar to:

a59c788 chore: checkpoint V1 data and shelter schema

But DO NOT assume this is correct.

Verify the actual commit history.

Determine:

- V1 initialization commit
- V1 data checkpoint commit
- subsequent physics commits
- V2 research commits
- when material IDs changed

Show a concise timeline.

============================================================
16. STEP TWELVE — CHECK WHETHER CURRENT MODIFICATION WAS INTENTIONAL
============================================================

Inspect commit messages and surrounding changes.

Determine whether MAT-* IDs were:

A. deliberately introduced as an architectural improvement

B. accidentally introduced

C. introduced by the thermal engine implementation

D. introduced by shelter schema implementation

E. introduced by another task

Do not speculate if Git history provides evidence.

If evidence is insufficient, explicitly say:

"Cause cannot be established from repository history."

============================================================
17. STEP THIRTEEN — DO NOT CONFUSE DATA FREEZING WITH API COMPATIBILITY
============================================================

Important architectural principle:

A frozen dataset should remain frozen.

If modern code needs a different identifier format, the correct solution
may be an adapter/mapping layer rather than modifying the historical data.

Investigate whether the architecture can support:

V1 frozen CSV
        ↓
Material ID adapter
        ↓
Canonical internal material ID
        ↓
Thermal engine

or:

V1 numeric ID
        ↓
mapping table
        ↓
MAT-* ID
        ↓
V2 / schema / engine

Do not implement it yet.

Evaluate the options first.

============================================================
18. STEP FOURTEEN — PROPOSE SAFE ARCHITECTURE OPTIONS
============================================================

After completing the investigation, compare these possible approaches:

OPTION A
Restore V1 CSV and change schema/engine to numeric IDs.

OPTION B
Keep modified CSV and declare it the new baseline.

OPTION C
Restore V1 CSV and add a compatibility mapping layer.

OPTION D
Keep V1 completely frozen and create a separate normalized material
registry for V1.1/V2.

OPTION E
Other architecture if justified by evidence.

For each option evaluate:

- V1 integrity
- backward compatibility
- V2 compatibility
- code complexity
- data traceability
- scientific credibility
- hackathon demo risk
- future extensibility

Recommend ONE option.

============================================================
19. STEP FIFTEEN — DO NOT FIX YET
============================================================

At the end of the audit:

DO NOT make changes.

Instead produce exact instructions for the next action.

If restoration is recommended, identify:

- exact Git commit
- exact file
- exact safe command

But DO NOT execute the command.

If an adapter is recommended, specify:

- proposed file location
- responsibility
- input
- output
- mapping strategy
- tests required

But DO NOT create it.

============================================================
20. REQUIRED FINAL REPORT FORMAT
============================================================

Return the following report:

# THERMOSHELTER V1 BASELINE FORENSIC AUDIT

## 1. Executive Summary

Maximum 10 bullet points.

## 2. Git Timeline

Table:

| Commit | Date | Change | Impact |
|---|---|---|---|

## 3. Original V1 Material Dataset

Include:

- original commit
- ID format
- row count
- column count
- physical-property integrity

## 4. Current Material Dataset

Describe current state.

## 5. Exact Diff

Table:

| Field | Original | Current | Changed? |
|---|---|---|---|

Only include meaningful differences.

## 6. Dependency Analysis

Table:

| File | Numeric IDs | MAT-* IDs | Dependency |
|---|---|---|---|

## 7. Shelter Schema Compatibility

Explain exactly what the schema expects.

## 8. Thermal Engine Compatibility

Explain exactly what the engine expects.

## 9. V2 Compatibility

Explain how V2 material and assembly datasets relate.

## 10. Root Cause

State what the evidence proves.

Separate:

PROVEN
from
INFERRED
from
UNKNOWN

## 11. Risk Assessment

Use:

CRITICAL
HIGH
MEDIUM
LOW

## 12. Architecture Options

Compare Options A-E.

## 13. Recommended Architecture

Give ONE recommendation.

Explain WHY.

## 14. Safe Recovery Plan

Give a numbered sequence.

IMPORTANT:
Do not execute it.

## 15. Commands That Would Be Used

Provide commands only as examples.
Do not run destructive commands.

## 16. Verification Plan

After the eventual fix, explain how to verify:

- V1 file restored
- thermal engine still works
- schema still validates
- V2 remains intact
- tests pass
- Git status is clean
- no frozen files accidentally changed

## 17. Evaluator Perspective

Explain how this issue affects:

- technical credibility
- data credibility
- physics credibility
- judge confidence
- demo risk

## 18. Final Verdict

Use exactly:

"Evaluator's Verdict: ..."

============================================================
21. IMPORTANT CONSTRAINT
============================================================

This is NOT a request to make the repository better yet.

This is a request to understand the repository precisely before making
changes.

DO NOT optimize.

DO NOT refactor.

DO NOT clean up.

DO NOT rename.

DO NOT rewrite.

DO NOT restore.

DO NOT commit.

DO NOT push.

AUDIT FIRST.

============================================================
22. FINAL QUESTION
============================================================

At the very end answer:

"Is the safest next step to restore the frozen V1 material CSV,
introduce an adapter/mapping layer, or use another architecture?"

Give one answer and justify it from repository evidence.

END OF AUDIT INSTRUCTIONS
================================================================