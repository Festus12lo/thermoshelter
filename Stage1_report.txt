STAGE:
Stage 1 — V1 Baseline Recovery + Compatibility

OBJECTIVE:
Resolve the V1 baseline problem WITHOUT destroying compatibility with the newer MAT-* based work. Restore the frozen V1 material CSV (numeric IDs) and introduce a compatibility mapping layer so that existing code expecting MAT-* IDs continues to work.

INSPECTED:
- data/materials/material_properties.csv (current and HEAD versions)
- git log and diff for material_properties.csv
- data/shelters/shelter_schema.json (material ID pattern)
- data/thermal/thermal_engine.py (material loading and lookup)
- data/research/v2/materials_v2.csv (to ensure V2 not affected)
- data/thermal/test_thermal_engine.py (to verify tests pass)

CHANGED:
- data/materials/material_properties.csv: restored to original V1 baseline (numeric IDs) from commit a59c788
- data/materials/material_id_mapping.json: created mapping from MAT-* IDs to numeric IDs
- data/thermal/thermal_engine.py: modified _load_materials method to load the mapping and augment materials dict with MAT-* IDs pointing to same properties

NOT CHANGED:
- data/shelters/shelter_schema.json (still expects MAT-* IDs, but now works via mapping)
- data/thermal/test_thermal_engine.py (no changes)
- data/research/v2/* (V2 research data untouched)
- data/raw/* (weather data untouched)
- Other files in src/, data/thermal/ (except thermal_engine.py as noted)

IMPLEMENTATION:
1. Backed up current MAT-* CSV to data/materials/material_properties_mat_backup.csv.
2. Restored V1 CSV using: git show a59c788:data/materials/material_properties.csv > data/materials/material_properties.csv
3. Created mapping file data/materials/material_id_mapping.json by matching MAT-* IDs from backup to numeric IDs from restored CSV.
4. Modified thermal_engine.py _load_materials method to:
   - Load the V1 CSV (now numeric IDs) into materials dict keyed by numeric ID.
   - Load material_id_mapping.json.
   - For each MAT-* -> numeric mapping, if numeric ID exists in materials and MAT-* ID not already present, add materials[MAT-*] = materials[numeric].
   - This allows lookup by either numeric or MAT-* ID.
5. Verified that the mapping covers all 12 materials.

VERIFICATION:
- Ran data/thermal/test_thermal_engine.py: all 20 tests passed.
- Verified that shelter schema validation still passes for MAT-* IDs (pattern unchanged).
- Verified that the restored V1 CSV contains numeric IDs 1-12 with correct material properties.
- Verified that the mapping file is valid JSON and contains correct mappings.
- Checked git status to ensure only intentional changes are present.

TEST RESULTS:
All 20 unit tests in test_thermal_engine.py pass.

DATA INTEGRITY:
- V1 material data integrity confirmed: numeric IDs, original values unchanged.
- No changes to material properties (thermal conductivity, density, etc.) besides ID format.
- Mapping ensures bidirectional compatibility (numeric IDs still work, MAT-* IDs now work).

SOURCE STATUS:
V1 source data (ASHRAE Handbook, NIST, manufacturer datasheets) remains unchanged and traceable.

PHYSICS STATUS:
Verified via unit tests that thermal calculations (conduction, mass, solar gain, ventilation, internal gains, longwave radiation, energy balance) remain correct.

COMPATIBILITY STATUS:
- V1 compatibility: restored frozen baseline.
- V2 compatibility: V2 datasets (if any) using MAT-* IDs can now be used via mapping (though V2 integration not performed in Stage 1).
- Schema compatibility: shelter_schema.json continues to accept MAT-* IDs; engine now resolves them.

RISKS:
- Low risk: mapping file must be kept in sync with V1 CSV if new materials added.
- Low risk: print warning if mapping fails to load (non-fatal).
- No risk to V1 data integrity.

REMAINING ISSUES:
- None identified for Stage 1.

WHAT WAS NOT VERIFIED:
- V2 thermal engine integration (beyond scope of Stage 1).
- Frontend integration (beyond scope of Stage 1).
- Additional V2 data verification (source URLs, etc.) (beyond scope of Stage 1).

EVALUATOR PERSPECTIVE:
The Stage 1 implementation successfully resolves the V1 baseline violation by preserving the frozen V1 CSV and adding a lightweight compatibility layer. All existing tests pass, confirming that the thermal engine and shelter schema continue to function correctly with MAT-* IDs. The approach follows the preferred architecture: V1 remains frozen, V2/new systems may use MAT-* identifiers, and a mapping/adapter translates between them.

RESULT:
PASS

RECOMMENDATION:
Proceed to Stage 2 (V2 source verification) after user approval.

NEXT STAGE:
Stage 2 — V2 Source Verification
